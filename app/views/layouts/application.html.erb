<!DOCTYPE html>
<html>
  <head>
    <title>xpensis - expense tracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= yield(:head) %>
  </head>

  <body id="<%= "#{controller_name}-#{action_name}" %>">
    <main class="pb-5" style="min-height:75vh;">
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
    <footer class="py-5">
      <div class="container">
        <p class="float-end"><a href onclick="document.body.scrollTop=0;document.documentElement.scrollTop=0;">Back to top</a></p>
        <p>&copy; <%= Time.now.year %> xpensis.xyz &bull; <a href="#">Privacy</a> &bull; <a href="#">Terms</a></p>
      </div>
    </footer>
  </body>
</html>
